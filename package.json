{
  "name": "upc-engine",
  "version": "0.1.0",
  "description": "Identify, normalize, and reformat UPC-A/UPC-E barcodes per POS scanner profile.",
  "type": "module",
  "bin": {
    "upc-engine": "./bin/upc-engine.mjs"
  },
  "main": "src/index.mjs",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "start": "node src/server/index.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
